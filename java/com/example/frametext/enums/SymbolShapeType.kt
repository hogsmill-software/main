package com.example.frametext.enums
// Android unfortunately converts some symbols into emojis, so if we want to set colour to symbol, we need to draw it
enum class SymbolShapeType {
    None,
    Circle,
    Star,
    Square,
    Heart,
    Spade,
    Club,
    Diamond,
    Smiley
}